# Undo changes to an unstaged file; this will wipe out all changes made to the unstaged file forever
git checkout -- file_name

# Undo changes to all unstaged files in the current directory and subdirectories
git checkout .
