# ! Do not create a Git repo inside another Git repo

git init new_repo_name
cd new_repo_name

# Check if the repo has been initialized correctly
git status

# Covert an exsiting project into a repo
# Run the following command from the project directory
git init
git status # Check if the repo has been initialized correctly
